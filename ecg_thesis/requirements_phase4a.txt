# Phase 4A uses ECG-FM through a Kaggle input copy of fairseq-signals.
# Prefer adding the fairseq-signals repository to PYTHONPATH in Kaggle instead
# of running `pip install -e`, because editable install can disturb Kaggle's
# preinstalled CUDA/RAPIDS dependency set.
#
# If fairseq-signals import still fails, install its missing dependencies in a
# clean environment or provide a Kaggle input that already contains them.
